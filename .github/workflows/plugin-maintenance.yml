name: Engage Web Plugin Maintenance

on:
  workflow_dispatch:
    inputs:
      release_version:
        description: New plugin version (for example, 2.1.1)
        required: true
        type: string
      confirm_release:
        description: Test, version, tag, and deploy this release to WordPress.org
        required: true
        type: boolean
        default: false

permissions:
  contents: write

concurrency:
  group: dynaparent-wordpress-org-release
  cancel-in-progress: false

jobs:
  release:
    name: Check and prepare release
    if: github.ref == 'refs/heads/main' && inputs.confirm_release
    uses: Engage-Web/github-actions/.github/workflows/wordpress-plugin-release.yml@main
    with:
      plugin_slug: dynaparent
      plugin_file: dynparent-plugin.php
      plugin_name: Dynaparent
      release_version: ${{ inputs.release_version }}
      profile: legacy
    secrets: inherit
